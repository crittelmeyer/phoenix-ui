---
phase: 04-documentation-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - apps/storybook/stories/Button.stories.tsx
  - apps/storybook/stories/Input.stories.tsx
  - apps/storybook/stories/Textarea.stories.tsx
  - apps/storybook/stories/Select.stories.tsx
  - apps/storybook/stories/Checkbox.stories.tsx
  - apps/storybook/stories/RadioGroup.stories.tsx
  - apps/storybook/stories/Dialog.stories.tsx
autonomous: true

must_haves:
  truths:
    - 'Each of the 7 components has a story visible in the Storybook sidebar'
    - 'Button story shows all variants (default, destructive, outline, secondary, ghost, link) and sizes (default, sm, lg, icon)'
    - 'Compound components (Select, Dialog) show fully assembled interactive examples'
    - 'Each component has a Disabled state story'
    - 'Auto-docs pages generate prop tables for all components'
  artifacts:
    - path: 'apps/storybook/stories/Button.stories.tsx'
      provides: 'Button stories with all variants, sizes, disabled'
      contains: 'satisfies Meta'
    - path: 'apps/storybook/stories/Input.stories.tsx'
      provides: 'Input stories with placeholder, disabled, types'
      contains: 'satisfies Meta'
    - path: 'apps/storybook/stories/Textarea.stories.tsx'
      provides: 'Textarea stories with autoResize, disabled'
      contains: 'satisfies Meta'
    - path: 'apps/storybook/stories/Select.stories.tsx'
      provides: 'Select stories with subcomponents, disabled'
      contains: 'subcomponents'
    - path: 'apps/storybook/stories/Checkbox.stories.tsx'
      provides: 'Checkbox stories with checked states, disabled'
      contains: 'satisfies Meta'
    - path: 'apps/storybook/stories/RadioGroup.stories.tsx'
      provides: 'RadioGroup stories with options, disabled'
      contains: 'satisfies Meta'
    - path: 'apps/storybook/stories/Dialog.stories.tsx'
      provides: 'Dialog stories with subcomponents, all parts'
      contains: 'subcomponents'
  key_links:
    - from: 'apps/storybook/stories/*.stories.tsx'
      to: '@phoenix/ui'
      via: 'named imports from barrel export'
      pattern: "from '@phoenix/ui'"
---

<objective>
Create one story file per component (7 total) with multiple named exports showing all variants, sizes, and states including disabled.

Purpose: Provides interactive documentation for every core component, satisfying DOCS-02 and completing DOCS-01.
Output: 7 story files covering Button, Input, Textarea, Select, Checkbox, RadioGroup, and Dialog.
</objective>

<execution_context>
@/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-documentation-infrastructure/04-RESEARCH.md
@.planning/phases/04-documentation-infrastructure/04-01-SUMMARY.md

Key source files to read for component APIs:

- packages/ui/src/components/button.tsx (variants: default/destructive/outline/secondary/ghost/link, sizes: default/sm/lg/icon, asChild prop)
- packages/ui/src/components/input.tsx (standard input props + forwarded ref)
- packages/ui/src/components/textarea.tsx (autoResize prop)
- packages/ui/src/components/select.tsx (10 compound parts)
- packages/ui/src/components/checkbox.tsx (Radix checkbox)
- packages/ui/src/components/radio-group.tsx (RadioGroup + RadioGroupItem)
- packages/ui/src/components/dialog.tsx (10 compound parts)
- packages/ui/src/index.ts (barrel exports - all named exports)
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create stories for simple components (Button, Input, Textarea, Checkbox)</name>
  <files>
    apps/storybook/stories/Button.stories.tsx
    apps/storybook/stories/Input.stories.tsx
    apps/storybook/stories/Textarea.stories.tsx
    apps/storybook/stories/Checkbox.stories.tsx
  </files>
  <action>
    Read each component source file first to understand exact props and variants. Import all components from '@phoenix/ui' (barrel export).

    Use CSF 3.0 format for all stories:
    - `const meta = { ... } satisfies Meta<typeof Component>`
    - `export default meta`
    - `type Story = StoryObj<typeof meta>`
    - Include `tags: ['autodocs']` in every meta (Pitfall 10 from research)
    - Use `parameters: { layout: 'centered' }` for all components

    **Button.stories.tsx:**
    - Read packages/ui/src/components/button.tsx for exact variants and sizes
    - argTypes with control: 'select' for variant and size enums
    - Stories: Default, Destructive, Outline, Secondary, Ghost, Link, AllVariants (render all in flex row), AllSizes (render all sizes), Disabled, WithIcon (button with an arrow or similar text icon)
    - title: 'Components/Button'

    **Input.stories.tsx:**
    - Read packages/ui/src/components/input.tsx for props
    - Stories: Default (placeholder text), WithValue (defaultValue set), Disabled, Password (type="password"), Email (type="email"), WithLabel (wrap Input with Label component from @phoenix/ui)
    - title: 'Components/Input'

    **Textarea.stories.tsx:**
    - Read packages/ui/src/components/textarea.tsx for props (especially autoResize)
    - Stories: Default, WithPlaceholder, Disabled, AutoResize (autoResize={true} with defaultValue showing multi-line text), WithLabel
    - title: 'Components/Textarea'

    **Checkbox.stories.tsx:**
    - Read packages/ui/src/components/checkbox.tsx for props
    - Stories: Default, Checked (defaultChecked={true}), Disabled, DisabledChecked (disabled + defaultChecked), WithLabel (pair with Label)
    - title: 'Components/Checkbox'

  </action>
  <verify>
    - All 4 story files exist in apps/storybook/stories/
    - Each file exports a default meta with `tags: ['autodocs']`
    - Each file has at least Default and Disabled named exports
    - Start Storybook and confirm stories appear in sidebar under "Components/"
  </verify>
  <done>
    Button, Input, Textarea, and Checkbox each have story files with all variants/sizes/states documented. Auto-docs pages generate prop tables. Controls panel allows interactive prop toggling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stories for compound components (Select, RadioGroup, Dialog)</name>
  <files>
    apps/storybook/stories/Select.stories.tsx
    apps/storybook/stories/RadioGroup.stories.tsx
    apps/storybook/stories/Dialog.stories.tsx
  </files>
  <action>
    Read each component source file for exact exported parts. Import from '@phoenix/ui'. Use subcomponents property for compound components (Pitfall 2 from research).

    **Select.stories.tsx:**
    - Read packages/ui/src/components/select.tsx for all 10 exported parts
    - meta with component: Select, subcomponents: { SelectTrigger, SelectContent, SelectItem, SelectValue, SelectGroup, SelectLabel, SelectSeparator }
    - Stories: Default (3 fruit items), WithGroups (using SelectGroup + SelectLabel for grouped options), Disabled (disabled prop on Select root), WithPlaceholder
    - All render functions show fully assembled Select with Trigger, Content, and Items
    - title: 'Components/Select'

    **RadioGroup.stories.tsx:**
    - Read packages/ui/src/components/radio-group.tsx for RadioGroup and RadioGroupItem
    - meta with component: RadioGroup, subcomponents: { RadioGroupItem }
    - Stories: Default (3 radio options with Labels), WithDefaultValue (defaultValue set), Disabled, Horizontal (className="flex flex-row gap-4" on RadioGroup)
    - Each RadioGroupItem paired with Label for accessibility
    - title: 'Components/RadioGroup'

    **Dialog.stories.tsx:**
    - Read packages/ui/src/components/dialog.tsx for all 10 exported parts
    - meta with component: Dialog, subcomponents: { DialogTrigger, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription, DialogClose, DialogOverlay }
    - Stories: Default (Button trigger, content with title/description/close button), WithForm (dialog containing Input + Label + Button footer), LargeContent (dialog with scrollable long text to test overflow), CustomSize (dialog with max-w-lg via className)
    - All stories must use DialogTrigger with a Button child so the dialog can be opened interactively
    - title: 'Components/Dialog'

  </action>
  <verify>
    - All 3 story files exist in apps/storybook/stories/
    - Each file has subcomponents property in meta
    - Each file has `tags: ['autodocs']`
    - Start Storybook and confirm compound components render correctly
    - Select dropdown opens and shows items
    - Dialog opens via trigger button and shows content
  </verify>
  <done>
    Select, RadioGroup, and Dialog have story files with subcomponents documented, fully assembled interactive examples, and disabled states. Auto-docs pages show prop tables for all sub-parts.
  </done>
</task>

</tasks>

<verification>
1. 7 story files exist in apps/storybook/stories/
2. All stories appear in Storybook sidebar under "Components/"
3. Each component has auto-generated Docs page with prop tables
4. Button story shows all 6 variants and 4 sizes
5. Select and Dialog stories render fully assembled compound examples
6. Dark mode toggle correctly themes all component stories
7. All components import from '@phoenix/ui' barrel export
</verification>

<success_criteria>

- 7 .stories.tsx files in apps/storybook/stories/
- Every component has Default and Disabled stories at minimum
- Compound components use subcomponents property for sub-part documentation
- All stories use CSF 3.0 with `satisfies Meta<typeof Component>` pattern
- DOCS-01 fully satisfied (Storybook renders all components)
- DOCS-02 fully satisfied (stories show all variants and sizes)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-infrastructure/04-02-SUMMARY.md`
</output>
