---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/tsconfig.node.json
  - apps/web/vite.config.ts
  - apps/web/eslint.config.mjs
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/index.css
  - apps/web/src/routes/root.tsx
  - apps/web/src/routes/components.tsx
  - apps/web/src/vite-env.d.ts
autonomous: true

must_haves:
  truths:
    - 'pnpm dev starts Vite dev server at localhost:5173'
    - 'Welcome page renders with Phoenix branding and link to Storybook'
    - 'React Router navigation works between / and /components routes'
    - 'Tailwind CSS utility classes render correctly (no PostCSS config needed)'
    - 'ESLint warns (not errors) on arbitrary Tailwind values in apps/web'
  artifacts:
    - path: 'apps/web/vite.config.ts'
      provides: 'Vite config with React + Tailwind CSS 4 plugins'
      contains: '@tailwindcss/vite'
    - path: 'apps/web/src/App.tsx'
      provides: 'React Router route definitions'
      contains: 'BrowserRouter'
    - path: 'apps/web/src/routes/root.tsx'
      provides: 'Welcome page with Phoenix branding'
      contains: 'Phoenix'
    - path: 'apps/web/eslint.config.mjs'
      provides: 'ESLint config with arbitrary values as warning (not error)'
      contains: 'no-arbitrary-value.*warn'
  key_links:
    - from: 'apps/web/src/main.tsx'
      to: 'apps/web/src/App.tsx'
      via: 'import and render'
      pattern: 'import.*App'
    - from: 'apps/web/src/index.css'
      to: 'tailwindcss'
      via: '@import directive'
      pattern: '@import "tailwindcss"'
    - from: 'apps/web/package.json'
      to: '@phoenix/ui'
      via: 'workspace dependency'
      pattern: "workspace:\\*"
---

<objective>
Create the apps/web Vite application with React Router 7, Tailwind CSS 4, and a welcome page.

Purpose: Provide the working dev server that validates the entire monorepo pipeline. When a developer runs `pnpm dev`, they see a Phoenix welcome page at localhost:5173, proving the foundation works end-to-end.

Output: A fully functional Vite + React + React Router + Tailwind CSS 4 application with welcome page and basic route skeleton.
</objective>

<execution_context>
@/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/web Vite application with React Router and Tailwind CSS 4</name>
  <files>
    apps/web/package.json
    apps/web/tsconfig.json
    apps/web/tsconfig.app.json
    apps/web/tsconfig.node.json
    apps/web/vite.config.ts
    apps/web/eslint.config.mjs
    apps/web/index.html
    apps/web/src/main.tsx
    apps/web/src/App.tsx
    apps/web/src/index.css
    apps/web/src/vite-env.d.ts
  </files>
  <action>
    Create the apps/web application from scratch (do NOT use `pnpm create vite` — write files directly for full control).

    **apps/web/package.json:**
    - name: "@phoenix/web", version: "0.0.0", private: true, type: "module"
    - scripts: dev: "vite", build: "tsc -b && vite build", preview: "vite preview", lint: "eslint .", typecheck: "tsc --noEmit"
    - dependencies: react, react-dom, react-router, @phoenix/ui (workspace:*), @phoenix/tokens (workspace:*)
    - devDependencies: @phoenix/typescript-config (workspace:*), @phoenix/eslint-config (workspace:*), @vitejs/plugin-react, vite, @tailwindcss/vite, tailwindcss, typescript, @types/react, @types/react-dom, eslint

    **apps/web/vite.config.ts:**
    - Import and use plugins: react() and tailwindcss() from @tailwindcss/vite
    - resolve.alias: '@' -> '/src'
    - Use `defineConfig` from vite

    **apps/web/tsconfig.json:**
    - References to tsconfig.app.json and tsconfig.node.json (Vite standard pattern)

    **apps/web/tsconfig.app.json:**
    - extends: "@phoenix/typescript-config/react.json"
    - compilerOptions: composite: true, baseUrl: ".", paths: { "@/*": ["./src/*"] }
    - include: ["src"]
    - references: [{ path: "../../packages/ui" }, { path: "../../packages/tokens" }]

    **apps/web/tsconfig.node.json:**
    - extends: "@phoenix/typescript-config/base.json"
    - compilerOptions: composite: true, module: "ESNext", moduleResolution: "Bundler"
    - include: ["vite.config.ts"]

    **apps/web/eslint.config.mjs:**
    - Import baseConfig from @phoenix/eslint-config/react.mjs
    - Spread baseConfig, then override: tailwindcss/no-arbitrary-value to "warn" (not error, apps are less strict)

    **apps/web/index.html:**
    - Standard Vite HTML: <!DOCTYPE html>, div#root, script type="module" src="/src/main.tsx"
    - Title: "Phoenix Design System"

    **apps/web/src/main.tsx:**
    - Import React, ReactDOM, BrowserRouter from react-router, App, index.css
    - Render App inside StrictMode + BrowserRouter

    **apps/web/src/App.tsx:**
    - Import Routes, Route from react-router
    - Import Root from @/routes/root and Components from @/routes/components
    - Define routes: "/" -> Root, "/components" -> Components

    **apps/web/src/index.css:**
    - `@import "tailwindcss";`
    - No PostCSS config needed with @tailwindcss/vite plugin

    **apps/web/src/vite-env.d.ts:**
    - `/// <reference types="vite/client" />`

    After creating files, run `pnpm install` from root to install all web app dependencies.

  </action>
  <verify>
    Run from project root:
    1. `pnpm install` succeeds
    2. `pnpm --filter @phoenix/web typecheck` passes
    3. `cat apps/web/vite.config.ts | grep tailwindcss` shows Tailwind plugin
  </verify>
  <done>
    apps/web exists as a Vite + React + Tailwind CSS 4 application. TypeScript compiles. Vite config has both React and Tailwind plugins. ESLint config extends shared config with arbitrary values downgraded to warning.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create welcome page and route skeleton</name>
  <files>
    apps/web/src/routes/root.tsx
    apps/web/src/routes/components.tsx
  </files>
  <action>
    Create route components demonstrating React Router patterns.

    **apps/web/src/routes/root.tsx:**
    - Welcome page with:
      - Dark background (bg-slate-900 or similar Tailwind utility)
      - Phoenix logo (use an SVG flame/phoenix icon inline, or a simple text-based logo using Tailwind)
      - Project name "Phoenix" in large text
      - Subtitle: "Design System Starter"
      - Link to Storybook: "View Storybook" (href to localhost:6006, opens in new tab)
      - Link to /components route: "Browse Components" (uses React Router Link)
      - Brief description text explaining the project
    - Use Tailwind utility classes for all styling (no inline styles)
    - Center content vertically and horizontally (min-h-screen flex items-center justify-center)
    - Responsive: looks good on mobile and desktop

    **apps/web/src/routes/components.tsx:**
    - Simple placeholder page showing "Components" heading
    - Text: "Components will appear here as they are built in Phase 3."
    - Link back to home route using React Router Link
    - Use Tailwind utility classes for styling

    After creating route files, verify dev server starts:
    1. Start dev server: `pnpm --filter @phoenix/web dev` (run briefly to confirm it starts)
    2. Or use `pnpm turbo run build --filter=@phoenix/web` to verify build succeeds

  </action>
  <verify>
    Run from project root:
    1. `pnpm --filter @phoenix/web build` completes successfully
    2. Route files exist and contain React Router Link imports
    3. `grep -r "inline\|style=" apps/web/src/routes/` returns no inline style usage
  </verify>
  <done>
    Welcome page renders with Phoenix branding, Storybook link, and component browser link. /components route shows placeholder. All styling uses Tailwind utility classes (no inline styles). React Router navigation works between routes. `pnpm dev` starts Vite dev server successfully at localhost:5173.
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds from root
2. `pnpm --filter @phoenix/web build` completes without errors
3. `pnpm --filter @phoenix/web typecheck` passes
4. `pnpm --filter @phoenix/web lint` runs (may have warnings, should not error on app code)
5. apps/web/src/routes/root.tsx contains "Phoenix" text and Tailwind classes
6. apps/web/eslint.config.mjs has no-arbitrary-value set to "warn"
7. apps/web/src/index.css contains `@import "tailwindcss"`
</verification>

<success_criteria>

- Vite dev server starts at localhost:5173
- Welcome page shows Phoenix branding with Storybook link
- React Router navigates between / and /components
- Tailwind CSS 4 utility classes render correctly
- TypeScript compiles with shared strict config
- ESLint config warns (not errors) on arbitrary Tailwind values
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
