/**
 * Phoenix Design Tokens
 *
 * Primary consumables are the generated CSS custom properties:
 * - Import light mode tokens: @phoenix/tokens/dist/tokens.css
 * - Import dark mode tokens: @phoenix/tokens/dist/tokens.dark.css
 *
 * CSS variables follow shadcn/ui semantic token convention:
 * --background, --foreground, --primary, --primary-foreground, etc.
 */

export {}
